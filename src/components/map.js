import { React, useState } from "react"

export function Map () {
    return (
        <>
            <p>Use an open-source map API and dataset; also, the map is hard, it has to cache and be interactive, etc.</p>
        </>
    );
}