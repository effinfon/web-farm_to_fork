import { React, useState } from "react"

export function SearchBar () {
    return (
      <>
        <p>I don't even know what HTML to use here; I guess it will be a flexbox, a textarea, some CSS styling and an img.</p>
      </>
    );
}